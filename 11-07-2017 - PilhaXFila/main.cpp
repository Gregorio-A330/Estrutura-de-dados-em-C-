#include <iostream>
#include <stack>
#include "PilhaXFila.h"

using namespace std;


int main()
{

    srand (time(NULL))
    for (int i=0;i<50;i++){
        cout<<(rand()%101) + 50<<endl;
    }
    while(f.filavazia){
        if(p.Pilhavazia)||f.primeiro<p.elementopo()
            p.empilha(f.remover());
            else
                f.insere(f.desempilha());
    }
    return 0;
}

